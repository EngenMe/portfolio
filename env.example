# NODEMAILER CONFIGURATION
SMTP_HOST=<your-smtp-host>      # The SMTP server hostname, e.g., smtp.gmail.com
SMTP_PORT=<your-smtp-port>      # The SMTP server port, e.g., 587 for STARTTLS
SMTP_USER=<your-smtp-username>  # Your SMTP username, usually your email address
SMTP_PASS=<your-smtp-password>  # Your SMTP password or app-specific password
RECEIVER_EMAIL=<receiver-email> # The email address to receive contact form submissions
